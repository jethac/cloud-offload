"""Provider specs shipped with Cloud Offload.

Each ``*.json`` file here is a declarative provider spec (see
``cloud_offload.providers.declarative``).  They are registered by default so a
provider served declaratively is never lost just because the user has no
``~/.cloud-offload/providers`` directory.
"""
